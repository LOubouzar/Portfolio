import React from "react";
import "./wrapper.css";
import Nav from "../nav";
import Hero from "../hero";

const Wrapper = () => (
    <div id="heroWrap">
        <Nav />
        <Hero />
    </div>
)

export default Wrapper;